define(['modules/ds'], function(ds) {
	ds.controller('TaskController', function ($scope, $http) {
		$scope.tasklist = [{
			title: "title",
			tip: "tip",
			reward: "reward"	
		}]

	  	var reset = function() {
			
		}
	});
});